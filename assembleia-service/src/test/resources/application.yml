spring:
  application:
    name: service
  profiles:
    active: test
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:assembleia
    username: assembleia
    password: assembleia
server:
  port: 8000
